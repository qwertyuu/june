import Tests;

// -- Test to make sure that the compiler
//    can find the primary record from another
//    file.
import Data;

main() {
	d :Data = Data(
		a = 14,
		b = 56,
	);

	Tests.print(d.a);
	Tests.print(" ");
	Tests.print(d.b);
}